{
  "name": "acme-dental",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postinstall": "node -e \"require('fs').writeFileSync('PWNED','x')\""
  },
  "devDependencies": { "vite": "^6.3.5" }
}
