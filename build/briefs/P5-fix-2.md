# Follow-up brief — Phase 5 hardening, round 2 (Codex `REVIEWED: P5 issues #r2`, 2026-08-30)

Round 1 implemented the *original* item 2 of `P5-fix.md`, not the hardened version (sections 2a–2f as they now read). Re-read `build/briefs/P5-fix.md` **as it is now** and implement it literally. Commits `P5-fix2: …`, one per item below, each with a test that fails before and passes after. Acceptance = the orchestrator's isolated `make verify` (Codex's sandbox cannot run `sandbox-exec`, so its reviews will judge code + tests only).

1. **Phase-specific deny-default sandbox profiles** (`website_gate.py:179`, `system/gates/sandbox/*.sb`): replace the `(allow default)` profiles. Build, preview and Chrome/Lighthouse phases must be `(deny default)` with explicit allowances per `P5-fix.md` 2e (temp tree, npm cache, node realpath, minimal system paths, `file-read-metadata`, `TMPDIR` inside the temp tree — **no** `/private/var/folders`, **no** blanket `/private/tmp`); network: build none; preview bind on the chosen port only; Chrome/Lighthouse outbound to the preview port + local devtools port only. The install phase keeps allow-default + write confinement + `$HOME` read denial + network. Record the final profiles in `system/gates/sandbox/{install,build,preview,lighthouse}.sb` with the port placeholders substituted at runtime. Iterate until Chrome actually launches under the Lighthouse profile — this is the hard part; `--user-data-dir` and `TMPDIR` inside the temp tree, `--no-sandbox --disable-gpu --headless=new --remote-debugging-port=<devtools port>`; if a Chrome subsystem needs an extra `mach-lookup`/`file-read*` allowance, add the narrowest one and note it in the profile with a comment. Test: `test_sandbox_denies_home_write_and_network` must run a **real** build under the build profile that attempts to read `/etc/hosts` and `/Users/moeghashim/.gitconfig`, write `$HOME/pwned`, and `fetch` `https://example.com` — all denied; plus the Lighthouse-profile unrelated-port test (a helper HTTP server on another loopback port started **outside** the sandbox must receive **no** request from page JS).
2. **Vetted vite install with integrity** (`website_gate.py:324`): add `system/gates/vite_versions.json` (`{"5": {"version": "5.x.y", "integrity": "sha512-…"}, "6": …, "7": …}` — record the values you obtain from `npm view vite@<exact> version dist.integrity` in PROGRESS.md), `npm pack vite@<exact> --pack-destination <cache>` (install profile), sha512 verification of the tarball against the allowlist (mismatch → `build_ok` fails `vite integrity mismatch`), `npm install --ignore-scripts --no-audit --no-fund --no-package-lock <tarball>`, then assert `node_modules/vite/package.json` name/version. Remove `npm view` range resolution from the gate. Tests: tampered tarball → fails; wrong version in `node_modules` → fails.
3. **lstat copy-out** (`website_gate.py:371`): replace `shutil.copytree` with an explicit walk: `os.lstat` every entry; accept only regular files and directories; reject symlinks, hard links (`st_nlink > 1`), sockets, FIFOs, devices (`build_ok` fails `unsafe artifact: <path>`); enforce realpath containment inside the temp `dist/`; limits 10 MB/file, 50 MB total; copy with `shutil.copyfile` (never follows links). Fixture `fail_build_artifact_symlink` (build output containing a symlink via a `public/` symlink).
4. **Dedicated Chrome/Lighthouse policy + real regression test** (`website_gate.py:809`, `test_website_gate.py:243`): Lighthouse runs under `lighthouse.sb` (item 1), Chrome launched with a fresh `--user-data-dir` in the temp tree and `--host-resolver-rules="MAP * ~NOTFOUND, EXCLUDE 127.0.0.1"`, 120 s timeout; the test must launch real Chrome under the profile (no empty `BuildCtx` stubs) and assert numeric `performance=… accessibility=…` scores on `pass`. Remove the relabel-only path unless it also surfaces the sandbox denial reason.
5. **URL validation order** (`website_gate.py:487`, `:657`): implement `P5-fix.md` item 4 steps (1)–(6) exactly, in one shared routine used by HTML/CSS references **and** IMAGE_BRIEF paths: split query/fragment → classify (external/protocol-relative ignored for links, forbidden for IMAGE_BRIEF) → percent-decode once, rejecting malformed escapes → reject NULs, backslashes, residual encoded separators, and every raw or decoded `..` segment → normpath → realpath containment. Fixture `fail_links_escape` must cover `%2e%2e/`, `..%2f`, `%zz`, a backslash path, and an IMAGE_BRIEF path with `..`.
6. **srcset parsing** (`website_gate.py:465`): parse `srcset` per the HTML spec (candidates separated by commas **followed by whitespace or end**, URLs may contain commas as in `data:` URIs); ignore `data:` candidates. Test with a `data:image/svg+xml,…` candidate containing commas → not flagged.
7. **Exactly one vite dependency** (`website_package_schema.json:21`): the schema (or a post-validation check) must reject `vite` appearing in both `dependencies` and `devDependencies`, and reject any dependency other than `vite` in either. Fixture `fail_build_dup_dep`.
8. **Adversarial coverage** (`test_website_gate.py:155`): add `fail_build_absolute_import` (source with `import x from "/etc/hosts?raw"` and an `/@fs/` URL → build fails or `dist/` contains no `/etc` content — assert both), the protected-file read assertion (item 1), and the Lighthouse unrelated-port test (item 1).
9. **Codex's environment**: `make verify` cannot run `sandbox-exec` inside Codex's review sandbox (`sandbox_apply: Operation not permitted`). Add `make verify-gates-nosandbox` that runs the website-gate suite with `TENWHY_GATE_NO_SANDBOX=1` **only when `TENWHY_DEV=1`** (same dev-only rule as the Lighthouse skip; production ignores it) so reviewers can still exercise checks 1–5 logic; document in PROGRESS.md that the sandboxed run is the orchestrator's acceptance.

Finish with `DONE P5-fix2 <hash…>` — only hashes in `git log`.
