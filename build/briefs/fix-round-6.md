# Follow-up brief — round 6 (orchestrator observations from the live engagement, 2026-08-31) — commits `P3-fix6: …`

1. **Daemon observability** (`system/orchestrator/orchestrator.mjs`, `loop_runner.mjs`, `adapters/pi.mjs`, `wiring.mjs`): the daemon printed nothing during a 40-minute live run — every diagnosis had to come from the DB. Add structured single-line logs to stdout (`ts level component msg key=value…`): tick start/end with counts; loop run start/finish with status; each executor/reviewer/prepare turn start + end (session id, exit code, duration, tool-call count); gate start/end with pass counts; adapter errors with the first 200 chars of stderr; absorb per-file progress; approvals processed; deploy start/end. No credential values ever. Add `TENWHY_LOG_LEVEL` (`info` default, `debug` adds prompts' first 200 chars). Test: run `tick` on a fixture and assert the expected log lines appear on a captured stdout.
2. **Faster knowledge absorption** (`system/orchestrator/knowledge.mjs`, `adapters/claude.mjs`): absorption currently makes one `claude -p` call per file (13 calls ≈ 10 min for 6 competitors + 4 products). Batch: one call that returns a JSON object `{ "<rel path>": "<synthesis body>" }` for all targets (prompt lists each target with its current body and the relevant RESEARCH.json slice); validate the JSON, fall back to per-file calls only for missing/invalid entries. Keep the deterministic frontmatter/History handling unchanged. Tests: fixture model returning the batch JSON → all files written with one model call; a batch reply missing one file → exactly one fallback call.
3. **Stale-daemon guard** (`cli.mjs daemon`): two daemons ran concurrently for ~1 minute today after a restart. Acquire an exclusive lock on `state/daemon.lock` (`O_EXCL` + pid + liveness check via `process.kill(pid, 0)`); a second `loopctl daemon` must exit 3 with `daemon already running (pid N)`. Test with a fake lock file holding a live pid vs a dead pid.

Finish with `DONE fix6 <hash…>` — only hashes in `git log`.
