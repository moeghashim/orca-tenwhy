# Follow-up brief — round 7 (Codex `REVIEWED: P3 issues #r7` on round 6, 2026-08-31) — commits `P3-fix7: …`, each with a failing-then-passing test

1. **Value-based redaction** (`system/orchestrator/log.mjs:30`): redacting by secret-looking *field names* is not enough — stderr tails, error messages, and `debug`-level prompt previews can carry credential **values**. Maintain a process-wide redaction set: every value loaded from provision env files / `TENWHY_*` secrets / `process.env` keys matching `(TOKEN|SECRET|KEY|PASSWORD|API_)` with length ≥ 12, plus any value the deploy step loads; replace occurrences (exact and URL-encoded) with `[redacted]` in **every** log line, event payload string, and thrown error message before emission. Test: seed a fake token into the redaction set, log a line containing it in a stderr-like string and in a prompt preview → both redacted; the same token inside an `Error.message` passed to the logger → redacted.
2. **Prepare turn log fields** (`system/orchestrator/loop_runner.mjs:399`): the `loop_run.prepared` / prepare-turn log line must include the adapter exit code, duration, session id and tool-call count (0 expected — the designer runs with no tools), exactly like executor/reviewer turns. Test asserts the fields.
3. **Race-free daemon lock** (`system/orchestrator/daemon_lock.mjs:50`): unlinking a stale/partial lock and re-creating it can admit two daemons. Use an atomic scheme: write `state/daemon.lock.<pid>.tmp` then `fs.linkSync(tmp, lock)` (fails with `EEXIST` if the lock exists); on `EEXIST` read the pid, `process.kill(pid, 0)` — if dead, `rename` the stale lock to `daemon.lock.stale.<ts>` (atomic) and retry once; never `unlink` a lock you do not own. Remove the lock on `SIGINT`/`SIGTERM`/normal exit only when its pid matches ours. Tests: (a) two acquirers racing (spawn two child processes that both try to acquire within 50 ms) → exactly one succeeds, the other exits 3; (b) stale lock with a dead pid → acquired, stale file renamed; (c) SIGINT to a holder removes its lock, and a lock owned by another pid is left untouched.

Finish with `DONE fix7 <hash…>` — only hashes in `git log`.
