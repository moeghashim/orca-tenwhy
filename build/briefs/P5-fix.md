# Follow-up brief — Phase 5 review fixes (do after Phase 6; commits `P5-fix: …`, each with a failing-then-passing test)

Source: orchestrator spot-check + Codex `REVIEWED: P5 … #r1` (items appended below when the verdict lands).

1. **Lighthouse skip is dev-only** (`system/gates/website_gate.py` `lighthouse_check`): `WEBSITE_GATE_SKIP_LIGHTHOUSE=1` currently yields `passed: true`. Honour it **only** when `TENWHY_DEV=1` is also set; otherwise ignore the variable and run Lighthouse. `make verify-fast` sets both. The runner's `gateRunner` (`system/orchestrator/wiring.mjs spawnGate`) must spawn gate scripts with a **scrubbed environment** that never forwards `WEBSITE_GATE_SKIP_LIGHTHOUSE` / `TENWHY_DEV` (test: set both in `process.env`, call `spawnGate` on a stub gate that echoes its env, assert neither key is present).

2. **No code execution from executor-controlled files** (Codex #r1 item 1 — SOP §0.3): the gate's `npm install` / `npm run build` executes whatever `website/package.json` scripts and `vite.config.*` contain. Fix in three layers, all tested:
   a. **Package manifest allowlist**: add gate check-2 pre-validation — `website/package.json` must match `system/gates/website_package_schema.json`: `dependencies`/`devDependencies` ⊆ `{ "vite" }` (any `^5`/`^6`/`^7` range), `scripts` exactly `{ "build": "vite build", "preview": "vite preview" }` (optional `dev`), **no** `postinstall`/`preinstall`/`prepare`/`install` or any other lifecycle key, no `overrides`, `workspaces`, `bin`, or `config`. Violations fail `build_ok` with the offending key before anything runs.
   b. **No executable config**: any of `vite.config.*`, `postcss.config.*`, `tailwind.config.*`, `.npmrc`, `.yarnrc*`, `*.config.{js,cjs,mjs,ts}` at `website/` root fails `build_ok` (detail `forbidden executable config: <file>`); the path guard (`path-guard.mjs`) also **blocks writes** to those names and to `package-lock.json` so the executor cannot introduce them (guard test). The gate writes a fixed `vite.config.mjs` **itself** (from `system/gates/vite.config.template.mjs`, `base: "./"`) into a temp copy of `website/` before building — never the executor's.
   c. **Build isolation**: `npm ci --ignore-scripts --no-audit --no-fund` (or `npm install --ignore-scripts …` when no lockfile — the gate deletes any executor-written lockfile first), with `npm_config_cache=<state/npm-cache>`; build runs in a **temp copy** of `website/` with `NODE_OPTIONS` cleared and a scrubbed env (only `PATH`, `HOME`, `npm_config_cache`); `dist/` is copied back for the remaining checks. Fixtures: `fail_build_scripts` (a `postinstall` that writes a marker file → check fails **and** the marker never appears), `fail_build_config` (`vite.config.js` present).
3. **CSS `url()` resolution** (#r1 item 2, `links_ok`): every `url(...)` in `dist/**/*.css` and inline `<style>` blocks (ignoring `data:`, `http(s):`, `#`) must resolve to a file under `dist/` (relative to the CSS file, or root-relative). Fixture `fail_links_css`.
4. **Path normalisation + containment** (#r1 item 3, `links_ok`): normalise every internal href/src/url with `posixpath.normpath` after joining, strip query/fragment, then require the resolved file to be inside `dist/` (realpath containment); `/../outside.png` or `../../x` counts as broken. Fixture `fail_links_escape` (a link to `/../` and a symlink in `dist/` pointing outside).
5. **Word-boundary copy grounding** (#r1 item 4, `copy_grounded`): match company name and product names as whole phrases — regex `(?<!\w)<escaped phrase>(?!\w)` on whitespace-normalised, case-folded visible text; names shorter than 3 characters never match. Fixture `fail_copy_substring` (text contains the names only inside longer words → fails) and extend `pass` with a name adjacent to punctuation (must still match).
6. **Robust environment for the gate** (#r1 item 5): the gate must not depend on the caller's npm cache or Chrome state: set `npm_config_cache` to `state/npm-cache/` (create it), pass `--chrome-flags="--headless=new --no-sandbox --disable-gpu"` to Lighthouse, and if Chrome cannot be launched fail `lighthouse≥85` with a clear `detail` (`chrome launch failed: …`) — never a Python traceback. Add a unit test that runs the gate with `HOME` pointed at an empty temp dir and asserts checks 1–4 still evaluate.

Finish with `DONE P5-fix <hash…>` — only hashes in `git log`.
