# Follow-up brief — Phase 5 review fixes (do after Phase 6; commits `P5-fix: …`, each with a failing-then-passing test)

Source: orchestrator spot-check + Codex `REVIEWED: P5 … #r1` (items appended below when the verdict lands).

1. **Lighthouse skip is dev-only** (`system/gates/website_gate.py` `lighthouse_check`): `WEBSITE_GATE_SKIP_LIGHTHOUSE=1` currently yields `passed: true`. Honour it **only** when `TENWHY_DEV=1` is also set; otherwise ignore the variable and run Lighthouse. `make verify-fast` sets both. The runner's `gateRunner` (`system/orchestrator/wiring.mjs spawnGate`) must spawn gate scripts with a **scrubbed environment** that never forwards `WEBSITE_GATE_SKIP_LIGHTHOUSE` / `TENWHY_DEV` (test: set both in `process.env`, call `spawnGate` on a stub gate that echoes its env, assert neither key is present).

2. **No code execution from executor-controlled files** (Codex #r1 item 1 + spec review #s3 — SOP §0.3). The gate never runs anything from the customer tree in place. Layers, all tested:
   a. **Manifest allowlist** — `system/gates/website_package_schema.json` with `additionalProperties: false` at every level: allowed top-level keys `name`, `private`, `version`, `type` (`"module"`), `scripts`, `dependencies`, `devDependencies`. `scripts` exactly `{ "build": "vite build", "preview": "vite preview" }` plus optional `"dev": "vite"`. Exactly one dependency, `vite`, whose value matches `^\^[567](?:\.\d+){0,2}$` (plain anchored caret range — rejects aliases `npm:`, tags, URLs, `git:`/`file:`/`workspace:` protocols); no `optionalDependencies`, `bundledDependencies`, `peerDependencies`, `overrides`, `workspaces`, `bin`, `config`, `engines`, lifecycle keys. Violations fail `build_ok` naming the key.
   b. **Forbidden files** — fail `build_ok` (`forbidden executable/config file: <path>`) if any of these exist anywhere under `website/`: `vite.config.*`, `*.config.{js,cjs,mjs,ts,mts,cts}`, `.postcssrc*`, `postcss.config.*`, `tailwind.config.*`, `.browserslistrc`, `browserslist.config.*`, `tsconfig*`, `jsconfig*`, `.env*`, `.npmrc`, `.yarnrc*`, `.pnpmfile*`, `pnpm-workspace.*`, `package-lock.json`, `yarn.lock`, `pnpm-lock.yaml`, `node_modules/`, `dist/` committed by the executor, and **any symlink** under `website/`. The path guard (`path-guard.mjs`) denies writes to all of these names/patterns and denies creating symlinks (guard tests).
   c. **Temp tree from allowlisted regular files only** — the gate builds a fresh temp tree under `/private/tmp/tenwhy-gate-<run>/` (outside `$HOME`): copy only **regular files** whose paths pass (b), never `node_modules`, `dist`, `.git`, sockets/devices/hard links/symlinks; `cd` into it; then write the **gate-owned** `vite.config.mjs` from `system/gates/vite.config.template.mjs`: `base: "./"`, and `build.rollupOptions.input` = every root-level `*.html` enumerated and sorted by the gate **before** the config is written (so multi-page sites build). Fresh `node_modules` are installed there.
   d. **Vetted, script-less install** — a gate-owned allowlist `system/gates/vite_versions.json` maps each accepted major (`5`, `6`, `7`) to **one exact vetted version and its sha512 `integrity`** (recorded by the implementer from `npm view vite@<exact> dist.integrity` and pasted into PROGRESS.md). The manifest's range only selects the major. The gate runs `npm pack vite@<exact> --pack-destination <npm cache>` (network allowed), verifies the tarball's sha512 against the allowlist (mismatch → `build_ok` fails `vite integrity mismatch`), then installs **from the verified tarball path** with `npm install --ignore-scripts --no-audit --no-fund --no-package-lock <tarball>` and finally asserts `node_modules/vite/package.json` `name`/`version` match before anything executes. Never `npm ci`/`npm install` on executor input. Environment for install/build/preview: `HOME` = gate-owned empty temp dir, `NPM_CONFIG_USERCONFIG`/`NPM_CONFIG_GLOBALCONFIG` = empty gate-owned files, `npm_config_cache` = `state/npm-cache/` (created), `NODE_OPTIONS` unset, `PATH` = `/opt/homebrew/bin:/usr/bin:/bin`, nothing else.
   e. **OS sandbox (`/usr/bin/sandbox-exec`, verified 2026-08-30 — environment.md addendum)** — every phase runs under `sandbox-exec -f <generated profile>` with `TMPDIR` set to a gate-owned directory inside the temp tree (no `/private/var/folders` allowance). Two profile classes:
      - **Install phase** (no executor code can run: manifest is allowlisted, `--ignore-scripts`, the only package is the integrity-verified vite tarball — the threat here is npm itself, not the executor): `(allow default)` + `(deny file-write*)` + `(allow file-write* (subpath "<temp tree>") (subpath "<npm cache>") (literal "/dev/null"))` + `(deny file-read* (subpath "/Users/moeghashim"))` + `(allow network*)`.
      - **Executor-code phases** (build; preview; Chrome/Lighthouse) — **deny by default**: `(deny default)`; allow `file-read*` only for the temp tree, the npm cache, `/opt/homebrew/bin/node` + `/opt/homebrew/Cellar/node*` (or the resolved realpath of the node binary), the Chrome app bundle (Lighthouse profile only), and the minimal system set (`/usr/lib`, `/usr/share`, `/System/Library`, `/private/etc/localtime`, `/dev/null`, `/dev/urandom`, `/dev/tty`); allow `file-read-metadata` broadly (needed for path resolution); allow `file-write*` only under the temp tree and `TMPDIR`; allow `process-exec` only for node (build/preview) or Chrome (Lighthouse) plus `process-fork`, `sysctl-read`, `mach-lookup` for the services those binaries need (iterate with the tests below until they pass — record the final profiles in `system/gates/sandbox/*.sb`). Network: **build** none; **preview** `(allow network-bind (local ip "localhost:<preview port>"))` only; **Chrome/Lighthouse** `(allow network-outbound (remote ip "localhost:<preview port>"))` + `(allow network* (local ip "localhost:<devtools port>"))` only — no other loopback port. Preview and Chrome/Lighthouse get **separate** profiles. The build is invoked as `node node_modules/vite/bin/vite.js build --config vite.config.mjs` (the freshly installed, verified binary — never `npm run`, `npx`, or anything from the original tree); `dist/` is copied back to the customer repo for checks 3–4 via an **lstat walk** that accepts only regular files and directories (symlinks, hard links (`nlink > 1`), sockets, devices → `build_ok` fails `unsafe artifact: <path>`), enforces containment and size limits (10 MB per file, 50 MB total), and copies without following links; check 5 serves the temp tree's `dist/` with `node node_modules/vite/bin/vite.js preview`. Lighthouse/Chrome run inside the loopback-only sandbox with a fresh `--user-data-dir` in the temp tree, `--host-resolver-rules="MAP * ~NOTFOUND, EXCLUDE 127.0.0.1"`, scrubbed env, 120 s timeout. If `sandbox-exec` is missing or the profile fails to load, `build_ok` fails with `sandbox unavailable` — never fall back to an unsandboxed run.
   f. **Fixtures/tests**: `fail_build_scripts` (a `postinstall` writing a marker → manifest rejected **and** marker absent), `fail_build_config` (`vite.config.js` present), `fail_build_symlink` (symlinked `node_modules/.bin/vite` and a `public/` symlink to `/etc` → rejected, nothing from `/etc` in `dist/`), `fail_build_alias` (`"vite": "npm:evil@1"`), `pass_multipage` (two root `*.html` both built), plus sandbox tests against the **real pipeline**: (i) `fail_build_absolute_import` — an executor source with `import x from "/etc/hosts?raw"` (and an `/@fs/` URL) must fail to build or, if Vite tolerates it, `dist/` must contain no `/etc` content (assert both); (ii) a stub build script under the build profile that tries to write `$HOME/pwned`, read `/Users/moeghashim/.gitconfig`, and fetch `https://example.com` → all fail (`EPERM` / no file / network error); (iii) under the Lighthouse profile, a page whose script `fetch`es `http://127.0.0.1:<unrelated port>` (a test server started outside the sandbox) must fail — assert the test server saw no request.
3. **CSS references** (#r1 item 2, #s3 item 8; `links_ok`): validate `url()` and both `@import` forms in external stylesheets, `<style>` blocks, and `style=""` attributes (ignore `data:`, `http(s):`, `//`, `#`), resolving relative references against the containing CSS/HTML file; also `srcset` candidates. Fixtures: `fail_links_css` (external CSS), `fail_links_inline` (inline style), `fail_links_srcset`.
4. **Normalisation + containment** (#r1 item 3, #s3 item 9, #s4 items 5–6; `links_ok` **and** IMAGE_BRIEF paths). Order of operations for every reference: (1) split off the raw query and fragment; (2) classify — `http(s):`, `mailto:`, `tel:`, `data:`, `javascript:` and protocol-relative `//host/…` are **ignored** for HTML/CSS link validation but **forbidden** in IMAGE_BRIEF paths; (3) percent-decode the path exactly once, rejecting malformed escapes; (4) reject NULs, backslashes, any residual encoded separator (`%2f`, `%5c`), and **any raw or decoded `..` segment** — before normalisation, so `/../outside.png` fails rather than normalising to `/outside.png`; (5) `posixpath.normpath` after joining against the containing file (or `dist/` root for root-relative); (6) resolve on disk and require realpath containment inside `dist/` (symlinks pointing outside count as broken). Fixture `fail_links_escape` (`/../outside.png`, `%2e%2e/`, `..%2f`, a backslash path, and a symlink).
5. **Whole-phrase copy grounding** (#r1 item 4, #s3 item 10; `copy_grounded`): visible text = NFC-normalised, whitespace-collapsed, case-folded body text excluding `script`, `style`, `template`, `noscript`, comments; match company name and product names as whole phrases with `(?<!\w)…(?!\w)`; no minimum length — instead report names of < 3 characters in `detail` as `ambiguous short name: <n>` (they still count when matched). Fixture `fail_copy_substring`; extend `pass` with a name adjacent to punctuation.
6. **Gate environment robustness** (#r1 item 5): covered by 2d/2e (gate-owned `HOME`, cache, configs); additionally, a Chrome launch failure fails `lighthouse≥85` with `detail = "chrome launch failed: …"` (never a traceback); unit test runs checks 1–4 with `HOME` pointed at an empty temp dir.

Finish with `DONE P5-fix <hash…>` — only hashes in `git log`.
