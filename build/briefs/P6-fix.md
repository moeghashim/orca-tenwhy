# Follow-up brief — Phase 6 fidelity + review fixes (commits `P6-fix: …`, each with a jsdom test where testable)

Source: orchestrator visual comparison of the seeded build vs `dashboard/design/Loop Graph.dc.html` (headless Chrome, 1440 px, 2026-08-30) + Codex `REVIEWED: P6 … #r1` (appended when it lands). Design files win on appearance — match the mock exactly for each item below.

1. **Sidebar wordmark** — mock: one mono line `tenwhy / loop graph` with the sans subtitle `operations console` beneath. Build shows `tenwhy /` wrapped onto two lines with subtitle `loop graph`. Fix text + no-wrap.
2. **Connection card** — mock: `● live` (green, mono) on the first line and `SSE /api/events` (muted, small) beneath — two lines total. Build wraps `live · SSE /api/events` and repeats `/api/events` a third time. Render exactly two lines; `reconnecting · retry n/5 in Ns` and `disconnected` + close time follow the same two-line pattern.
3. **Runs header summary** — mock: `Runs` followed by the muted line `8 engagements · 5 active · sorted by last event`. Build omits it. Add `${engagements.length} engagements · ${active} active · sorted by last event` (active = engagements with a `running`/`queued` loop run).
4. **needs_human banner** — mock: text `⚑ N runs need human input` + `every other loop is proceeding without you` on the left, `open failures →` pinned to the **right edge**. Build places the link inline. Flex with `justify-content: space-between`.
5. **Sidebar footer snapshot time** — mock: `snapshot 12:41:03 · v0.1.0`. Build shows `snapshot now`. Render the snapshot's `serverTime` as `HH:MM:SS` (server value, not the client clock; update on every snapshot refetch).
6. **Iteration header spacing** — `iteration 1 ✓ approve 7m ago` renders with the verdict badge touching the label. Match the mock: label, badge, and relative time separated by an 8 px gap.
7. **Seed realism** — `seed.mjs`: the `awaiting_approval` engagement's research run (`run_res_0143`) has no `gate_checks` rows, so the loop detail shows `0/0 passed`; seed the five research checks as passed for a `gate_passed` run, and give the `complete` engagement a website run with five passed checks so the pipeline strip and gate card look like the mock.

## Codex `REVIEWED: P6 issues #r1` (2026-08-30) — code fixes, each with a test that fails before and passes after

8. **In-place patching, not root re-render** (`dashboard/web/src/app.js:467`): `applyPatch` must update only the affected row/card/cell nodes; the table's row DOM nodes must keep identity across patches. Test (`live.test.mjs`): capture `row.isSameNode` / element references before a patch and assert the same nodes exist afterwards with updated text; assert `document.querySelectorAll('[data-row]').length` unchanged and order unchanged.
9. **Flash fade** (`store.js:56`): flashed row ids must be cleared after ~800 ms (timer or `animationend`), and the CSS transition must actually run (`transition: background-color 800ms ease-out`). Test with fake timers: flagged → 800 ms later not flagged.
10. **Reconnect cursor precedence** (`server.mjs:80`): when both `Last-Event-ID` and `since=` are present, use the **larger** id (never replay events the client already applied); document that `since=` is only a bootstrap hint. Test: connect with `Last-Event-ID: 20` and `since=5` → first event delivered has id 21.
11. **Superseded comparison removal** (`store.js:51`): when a patch carries `comparisons` for engagement E's newer `gate_passed` research run, drop any comparison keyed by E's older research run and render `comparison superseded by run_x` there. Test with two runs.
12. **Server time only** (`main.js:8`, `app.js:452`): relative timestamps and the "recent" (2-minute blue) rule must advance from `serverTime` (snapshot/patch `created_at`) using a monotonic offset captured at load — never `Date.now()`/`new Date()` for the reference; no silent client-clock fallback (render `—` if server time is unknown). Test: mock a skewed client clock (±1 h) and assert relative labels are unaffected.
13. **Single status source** (`style.css:48`, `app.js:113/178/317`): remove every hardcoded status colour outside `src/status.js`; CSS gets colours via CSS variables set from `status.js`/`tokens.json` at boot (`--status-running-fg` …), and app.js uses `statusOf()` only. Test: grep-style unit test that `style.css` and `app.js` contain no `#2563eb`/`#059669`/`#dc2626`/`#b45309`/`#71717a` literals and no `rgba(` status tints.
14. **No re-sort while connected** (`app.js:382`): Failures (and every list) keep insertion/order stable across patches; re-sort only on snapshot refetch after reconnect. Test: patch that changes a card's status → card order unchanged; after `reconnect()` → sorted.
15. Codex's item 8 (npm cache `EPERM` in its sandbox during the P5 gate) is addressed by P5-fix items 2d/6 (gate-owned `npm_config_cache`); no action here.

Finish with `DONE P6-fix <hash…>` — only hashes in `git log`.
