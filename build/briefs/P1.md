# Build brief — Phase 1: system repo scaffold & state schema

**Executor:** Grok (grok-4.6, high). **Orchestrator:** Claude Code. **Reviewer:** Codex.
Read `SOP-loop-graph.md` §0, §1, §3 and `system/config/environment.md` before starting. The SOP is authoritative; this brief only adds mechanics.

## Operating rules (non-negotiable)

1. Work at the repo root `/Users/moeghashim/orca/projects/orca-tenwhy`. This repo **is** the system repo — do not create a nested `loop-graph/` directory.
2. Mechanical truth: a task is done only when its verify command exits 0. Paste the real output into `PROGRESS.md` (your row only: Status, verify output, commit hash — leave the Review column as `_pending_`).
3. Never invent model strings, CLI names, or flags. Copy them from `system/config/environment.md`. If something you need is not there, stop and write the question in `PROGRESS.md` under "Blockers for Moe".
4. One commit per task (`P1.1: …`, `P1.2: …`, `P1.3: …`). Git identity is already configured (`Moe Ghashim <mohanadgh@gmail.com>`) — do not change it. **Do not push. Do not amend or rebase earlier commits.** Do not commit `state/*.db`, `node_modules/`, or `system/tools/.venv/` (already gitignored).
5. Do not modify `dashboard/design/**`, `SOP-loop-graph.md`, or `system/config/environment.md`.
6. No stubs presented as done. A placeholder is acceptable only where the SOP schedules the real implementation for a later phase (e.g. `bin/loopctl` prints usage and exits 2 until P3.1) and it must say so in its own output.

## P1.1 — Scaffold layout

Create the §3.1 tree (repo root = `loop-graph/` in the SOP):

```
README.md                      # what this repo is, roster, how to run `make verify`
PROGRESS.md                    # exists — update your rows only
Makefile                       # target `verify` (runs every test that exists so far; must exit 0)
package.json                   # root Node project, "type":"module", node>=20; deps added as needed
system/config/environment.md   # exists
system/config/loops.yaml       # P1.3
system/orchestrator/.gitkeep
system/loops/company-research/.gitkeep
system/loops/website/.gitkeep
system/tools/.gitkeep          # (.venv already here, gitignored)
system/gates/.gitkeep
system/db/schema.sql           # P1.2
system/db/migrate.sh           # P1.2
state/.gitkeep                 # orchestrator.db is gitignored
dashboard/server/.gitkeep
dashboard/web/.gitkeep
dashboard/design/              # exists — do not touch
templates/customer-repo/.gitkeep
bin/loopctl                    # executable; prints usage for `new|update|approve` and exits 2 with "not implemented until P3.1"
```

**Verify (paste both):**
- `git ls-remote origin` exits 0 (print `echo exit=$?` after it).
- `find . -path ./node_modules -prune -o -path ./.git -prune -o -type d -print | sort` shows every directory above.

## P1.2 — SQLite schema + migrate.sh

- `system/db/schema.sql`: the seven tables from SOP §3.2 **verbatim** (same names, columns, CHECK constraints, references). Use `CREATE TABLE IF NOT EXISTS` so migration is idempotent. Enable `PRAGMA foreign_keys = ON;` at the top.
- `system/db/migrate.sh <db-path>`: applies `schema.sql` with the `sqlite3` CLI, then prints the table list (`.tables`). Exit non-zero on any error. Must be re-runnable.
- `system/db/test_schema.sh`: creates a temp DB, runs `migrate.sh`, and asserts:
  1. exactly these 7 tables exist: `approvals engagements events gate_checks iterations loop_runs scrapes`;
  2. `INSERT INTO iterations (id, loop_run_id, n, reviewer_verdict) VALUES ('t1','r1',1,'maybe')` **fails** with a CHECK constraint error;
  3. `INSERT INTO engagements (id, status) VALUES ('e1','bogus')` **fails**;
  4. `INSERT INTO loop_runs (id, engagement_id, loop_name, attempt, status) VALUES ('r1','e1','company-research',0,'bogus')` **fails**;
  5. `INSERT INTO approvals (id, engagement_id, action) VALUES ('a1','e1','maybe')` **fails**;
  6. a valid row of each kind inserts successfully;
  7. running `migrate.sh` a second time on the same DB exits 0.
  Print `PASS <n>` per assertion and exit 0 only if all pass.
- Wire `bash system/db/test_schema.sh` into `make verify`.

**Verify (paste):** `bash system/db/test_schema.sh; echo exit=$?` → all `PASS`, `exit=0`. Also paste `make verify; echo exit=$?`.

## P1.3 — loops.yaml + lint

`system/config/loops.yaml` — values copied from `environment.md`:

```yaml
version: 1
caps:
  iteration_cap: 4          # executor↔reviewer rounds per loop run
  retry_cap: 2              # orchestrator retries (attempt 0..2)
roles:
  orchestrator: { cli: claude, model: claude-fable-5, effort: high, auth: oauth }
  reviewer:     { harness: pi, provider: openai-codex, model: gpt-5.6-luna, thinking: high, auth: oauth }
  executor:     { harness: pi, provider: xai, model: <read from environment.md P0.1 "PENDING" — if not yet recorded, put `grok-4.6` and add a comment `# TODO verify Pi xai catalog id after /login xai`>, thinking: high, auth: oauth }
loops:
  company-research:
    inputs: [idea, site_url]
    tools: [scrape]
    gate: system/gates/research_gate.py
    outputs: [research/RESEARCH.json, research/SOURCES.md, company/]
  website:
    inputs: [research/RESEARCH.json]
    depends_on: [company-research]
    tools: []
    gate: system/gates/website_gate.py
    outputs: [brand/, website/]
edges:
  - from: company-research
    to: website
```

`system/config/lint_loops.js` (Node ESM; add the `yaml` package to `package.json` and `npm install`): loads the file, asserts `caps.iteration_cap === 4`, `caps.retry_cap === 2`, every role has `model` and `auth: oauth`, every loop's `gate` path is a string, every edge references defined loops, and no edge cycles. Prints `loops.yaml ok` and exits 0; otherwise prints the failing assertion and exits 1. Add `node system/config/lint_loops.js` to `make verify`.

**Verify (paste):** `node system/config/lint_loops.js; echo exit=$?` and `make verify; echo exit=$?`.

## When finished

Final message must be exactly:

```
DONE P1.1 <hash> P1.2 <hash> P1.3 <hash>
```

followed by anything you could not complete and why.
